import cv2
import numpy as np
from google.colab.patches import cv2_imshow

img = cv2.imread('lab2.png', 0)

sx = cv2.Sobel(img, cv2.CV_64F, 1, 0, 3)
sy = cv2.Sobel(img, cv2.CV_64F, 0, 1, 3)
grad = cv2.magnitude(sx, sy)

sharp = cv2.add(img, grad.astype(np.uint8))

cv2_imshow(sharp)

